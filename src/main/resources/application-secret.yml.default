DATABASE:
  HOST: ${DATABASE_HOST}
  PORT: ${DATABASE_PORT}
  NAME: ${DATABASE_NAME}
  USERNAME: ${DATABASE_USERNAME}
  PASSWORD: ${DATABASE_PASSWORD}

jwt:
  secretKey: ${JWT_SECRET_KEY}

APP:
  ADMIN:
    EMAIL: ${MART_ADMIN_EMAIL}
    PASSWORD: ${MART_ADMIN_PASSWORD}